import { EmployeesDirective } from './employees.directive';

describe('EmployeesDirective', () => {
  it('should create an instance', () => {
    const directive = new EmployeesDirective();
    expect(directive).toBeTruthy();
  });
});
