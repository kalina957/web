import { DepartmentDirectiveDirective } from './department-directive.directive';

describe('DepartmentDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DepartmentDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
